//prompt("The Array Machine requires an array");
let theArray;
theArray = (5, 6, true);
let arrayMachine;
arrayMachine = function(theArray){
  let lastItem;
  lastItem = theArray;
};

$("#response").html(theArray);
